{
  "version": "1.1.58",
  "notes": "Fixed IMAGE→PDF image-read failures on Android/Google Photos. Selected images are now copied into an in-memory cache immediately after selection, and PDF generation uses robust object-URL + FileReader fallback loading."
}
